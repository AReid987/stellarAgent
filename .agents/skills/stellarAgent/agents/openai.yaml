interface:
  display_name: "StellarAgent"
  short_description: "Repo-specific patterns and workflows for stellarAgent"
  default_prompt: "Use the stellarAgent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true