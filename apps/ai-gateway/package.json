{
  "name": "@stellar-agent/ai-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pytest",
    "lint": "echo 'TODO: install GPTLint' && exit 1",
    "dev:trigger": "pnpm dlx trigger.dev@latest dev",
    "dev": "fastapi dev"
  },
  "dependencies": {
    "@trigger.dev/sdk": "^3.3.7"
  },
  "devDependencies": {
    "@trigger.dev/build": "^3.3.7"
  }
}
