# .envrc (in root directory)
layout python-venv
export PYTHONPATH="$PYTHONPATH:$PWD/apps/ai-gateway"

# Load the ai-gateway specific environment
if [ -f "apps/ai-gateway/.envrc" ]; then
    source_env apps/ai-gateway
fi